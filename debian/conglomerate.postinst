#! /bin/sh
 
set -e
 
PACKAGE=conglomerate
 
if [ "$1" = "configure" ]; then
  export GCONF_CONFIG_SOURCE=$(gconftool-2 --get-default-source)
  HOME=/root /usr/bin/gconftool-2 --makefile-install-rule \
  /etc/gconf/schemas/${PACKAGE}.schemas > /dev/null
fi
 
#DEBHELPER#
 
exit 0
 
# end of file
